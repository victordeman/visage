kind = "web"
previewPath = "/"
title = "VisageTrack AI Attendance"
version = "1.0.0"
id = "artifacts/visagetrack"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20983

[services.development]
run = "pnpm --filter @workspace/visagetrack run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/visagetrack", "run", "build" ]
publicDir = "artifacts/visagetrack/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20983"
BASE_PATH = "/"
